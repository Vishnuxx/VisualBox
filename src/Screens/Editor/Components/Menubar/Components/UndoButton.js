import { IconButton } from "../../../EditorUIControls/IconButton/IconButton";
import icon from "../Asset/undoIcon.png";

export function UndoButton(props) {
  return <IconButton icon={icon} label="undo" />;
}
