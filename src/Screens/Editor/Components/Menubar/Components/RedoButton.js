import { IconButton } from "../../../EditorUIControls/IconButton/IconButton";
import icon from "../Asset/redoIcon.png";

export function RedoButton(props) {
  return <IconButton icon={icon} label="redo" />;
}
