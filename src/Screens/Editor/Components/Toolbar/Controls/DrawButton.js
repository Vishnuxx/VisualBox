import { IconButton } from "../../../EditorUIControls/IconButton/IconButton";
import icon from "../Assets/draw.png";

export function DrawButton(props) {
  return <IconButton icon={icon} label="draw" />;
}
