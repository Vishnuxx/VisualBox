import { IconButton } from "../../../EditorUIControls/IconButton/IconButton";
import icon from "../Assets/line.png";

export function AddLineButton(props) {
  return <IconButton icon={icon} label="line" />;
}
